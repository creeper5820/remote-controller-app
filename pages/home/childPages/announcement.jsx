import { View, Text } from "react-native";

export default function Announcement({ navigation }) {
    {
        return (
            <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
                <Text style={{ fontSize: 30, color: 'black' }}>announcement</Text>
            </View>
        );
    }
}